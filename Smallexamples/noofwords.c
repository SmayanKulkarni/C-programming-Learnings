#include <stdio.h>
#include <string.h>

void main()
{
    char s[]="This is a string."
    int len=strlen(s);
    char nonwords[]=". ,;:\n\t";

    for(int i=0;i<len;i++)
    {
        for(int j=0;j<len;j++)
        {
            
        }
    }
}